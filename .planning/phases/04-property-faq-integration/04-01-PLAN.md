---
phase: 04-property-faq-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260303000001_add_checkin_checkout_house_rules.sql
  - src/app/api/chat/property-context.ts
  - src/app/api/chat/property-context.test.ts
autonomous: true
requirements:
  - FAQ-01
  - FAQ-03
  - FAQ-05

must_haves:
  truths:
    - "The properties table has checkin_time, checkout_time, and house_rules columns (nullable)"
    - "buildPropertyContext() serializes a property record to a structured string using only non-null fields"
    - "buildPropertyContext() maps amenity IDs to human-readable labels"
    - "buildPropertyContext() maps cancellation policy codes to full descriptions"
    - "buildPropertyContext() truncates description to 500 chars"
    - "buildPropertyContext() includes an explicit never-invent instruction at the end of the output"
    - "When checkin_time or house_rules are null, the context block omits those lines (does not write null)"
  artifacts:
    - path: "supabase/migrations/20260303000001_add_checkin_checkout_house_rules.sql"
      provides: "Schema migration adding checkin_time (text), checkout_time (text), house_rules (text) to properties table"
    - path: "src/app/api/chat/property-context.ts"
      provides: "buildPropertyContext() pure function, PropertyRecord interface, AMENITY_LABELS, CANCELLATION_LABELS"
      exports: ["buildPropertyContext", "PropertyRecord"]
    - path: "src/app/api/chat/property-context.test.ts"
      provides: "Unit test coverage for buildPropertyContext serialization"
  key_links:
    - from: "src/app/api/chat/property-context.ts"
      to: "src/app/api/chat/route.ts"
      via: "named export buildPropertyContext"
      pattern: "export function buildPropertyContext"
---

<objective>
Add the missing schema columns for check-in/out times and house rules, then build and test the
pure property context serialization function that converts a Supabase property row into a
structured system prompt block.

Purpose: FAQ-01 and FAQ-03 require real check-in/out times and house rules from the database.
Those columns do not yet exist. This plan adds them and creates the tested serialization layer
that all property FAQ answers depend on.

Output:
- SQL migration file with three new nullable columns
- property-context.ts with buildPropertyContext() and all label maps
- property-context.test.ts with full coverage of serialization behavior
</objective>

<execution_context>
@/Users/ninomarianolai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ninomarianolai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/chat/route.ts
@src/lib/admin-supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase migration — add checkin_time, checkout_time, house_rules columns</name>
  <files>supabase/migrations/20260303000001_add_checkin_checkout_house_rules.sql</files>
  <action>
    Create a Supabase SQL migration file that adds three nullable text columns to the properties table.

    The migration file must:
    - Add `checkin_time TEXT` — nullable. Stores human-readable time strings e.g. "15:00", "3:00 PM".
    - Add `checkout_time TEXT` — nullable. Same format as checkin_time.
    - Add `house_rules TEXT` — nullable. Free-text field for property-specific house rules.

    Use ALTER TABLE with IF NOT EXISTS guards so the migration is safe to re-run:

    ```sql
    -- Migration: add FAQ-required columns to properties table
    -- Phase 04 — Property FAQ Integration
    -- checkin_time and checkout_time store time-of-day as text (e.g. "15:00")
    -- house_rules stores free-text rules specific to the property
    ALTER TABLE properties
      ADD COLUMN IF NOT EXISTS checkin_time TEXT,
      ADD COLUMN IF NOT EXISTS checkout_time TEXT,
      ADD COLUMN IF NOT EXISTS house_rules TEXT;
    ```

    Run the migration with: `npx supabase db push` or `npx supabase migration up`

    Note: If the Supabase CLI is not configured locally, instruct the executor to apply it
    via the Supabase Dashboard SQL Editor as a fallback. The SQL itself is the deliverable.

    DO NOT use `NOT NULL` constraints — properties created before this migration have no values
    for these fields, and the chatbot handles null gracefully via FAQ-05.
  </action>
  <verify>
    <automated>test -f /Users/ninomarianolai/luxurystay/supabase/migrations/20260303000001_add_checkin_checkout_house_rules.sql && echo "migration file exists"</automated>
  </verify>
  <done>
    Migration file exists at supabase/migrations/20260303000001_add_checkin_checkout_house_rules.sql
    with ALTER TABLE adding checkin_time TEXT, checkout_time TEXT, house_rules TEXT columns.
    The SQL is idempotent (IF NOT EXISTS guards present).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: property-context.ts — pure serialization function with full test coverage</name>
  <files>src/app/api/chat/property-context.ts, src/app/api/chat/property-context.test.ts</files>
  <behavior>
    - Test: full property record → context string contains Name, Address, Amenities as human labels, Cancellation policy as full description, Check-in time, Check-out time, House rules
    - Test: amenity "wifi" → "Wi-Fi" in output (not raw ID)
    - Test: amenity "piscina" → "Piscina privata" in output
    - Test: cancellation_policy "flessibile" → full description string in output (not just "flessibile")
    - Test: cancellation_policy "rigida" → "Rimborso del 50% solo fino a 7 giorni prima" in output
    - Test: null description → line omitted from output entirely
    - Test: null checkin_time → line omitted from output entirely
    - Test: null house_rules → line omitted from output entirely
    - Test: description longer than 500 chars → truncated to 500 chars in output
    - Test: null amenities array → output contains "Amenities: not specified"
    - Test: empty amenities array → output contains "Amenities: not specified"
    - Test: output always ends with never-invent instruction containing "NEVER invent"
    - Test: output always ends with note about check-in/checkout times and house rules being unavailable when those fields are null
    - Test: propertyId UUID that is non-null → no crash, correct serialization
  </behavior>
  <action>
    RED phase: Write property-context.test.ts first. Import buildPropertyContext from './property-context'.
    Run tests — all must fail (file does not exist yet).

    GREEN phase: Create src/app/api/chat/property-context.ts with:

    1. PropertyRecord interface with all columns (include the new columns from Task 1):
       ```typescript
       export interface PropertyRecord {
         title: string | null
         description: string | null
         address: string | null
         lat: number | null
         lng: number | null
         price: number | null
         category: string | null
         space_type: string | null
         guests: number | null
         bedrooms: number | null
         beds: number | null
         bathrooms: number | null
         amenities: string[] | null
         cancellation_policy: string | null
         checkin_time: string | null
         checkout_time: string | null
         house_rules: string | null
       }
       ```

    2. AMENITY_LABELS constant (from research — confirmed from property/[id]/page.tsx):
       wifi→"Wi-Fi", tv→"TV", cucina→"Cucina", lavatrice→"Lavatrice",
       aria→"Aria condizionata", piscina→"Piscina privata",
       parcheggio→"Garage gratuito in loco", riscaldamento→"Riscaldamento",
       giardino→"Giardino", animali→"Animali domestici ammessi"

    3. CANCELLATION_LABELS constant (from research):
       flessibile → "Cancellazione gratuita fino a 24 ore prima del check-in. Rimborso del 50% nelle ultime 24 ore."
       moderata → "Cancellazione gratuita fino a 5 giorni prima. Rimborso del 50% tra 5 giorni e 24 ore prima."
       rigida → "Rimborso del 50% solo fino a 7 giorni prima del check-in. Nessun rimborso dopo."

    4. buildPropertyContext(property: PropertyRecord): string — builds context block:
       - Start with: "PROPERTY CONTEXT (use ONLY this data to answer property questions):"
       - For each field: only append line if value is truthy (not null, not undefined, not empty string)
       - Order: Name, Address, Coordinates (lat+lng together, only if BOTH non-null), Price per night,
         Maximum guests, Bedrooms, Beds, Bathrooms
       - Check-in time: if property.checkin_time → "Check-in time: {checkin_time}"
       - Check-out time: if property.checkout_time → "Check-out time: {checkout_time}"
       - Amenities: map IDs to AMENITY_LABELS[id] ?? id, join with ", "
         If amenities is null or empty array: "Amenities: not specified"
       - Cancellation policy: map via CANCELLATION_LABELS[id] ?? id
       - House rules: if property.house_rules → "House rules: {house_rules}"
       - Description: if property.description, truncate to 500 chars with:
         description.length > 500 ? description.slice(0, 500) + '...' : description
       - Always append blank line then:
         "IMPORTANT: If a property field is not listed above, say \"I don't have that information for this property.\" NEVER invent or guess."
       - If checkin_time is null OR checkout_time is null, also append:
         "Check-in/check-out times are not available for this property — acknowledge this honestly if asked."
       - If house_rules is null, also append:
         "House rules are not available for this property — acknowledge this honestly if asked."

    REFACTOR phase: Ensure no duplication, clear constant naming, no magic strings inline.

    Do NOT add 'use client' or any React imports — this is a server-only pure function module.
  </action>
  <verify>
    <automated>cd /Users/ninomarianolai/luxurystay && npx vitest run src/app/api/chat/property-context.test.ts</automated>
  </verify>
  <done>
    All property-context.test.ts tests pass.
    buildPropertyContext() correctly serializes all field combinations.
    Null fields are omitted. Labels are mapped. Description is truncated at 500 chars.
    Never-invent instruction always present at end of output.
  </done>
</task>

</tasks>

<verification>
1. Migration file exists and contains valid SQL with IF NOT EXISTS guards for all three columns
2. `npx vitest run src/app/api/chat/property-context.test.ts` — all tests pass
3. No existing tests broken: `npx vitest run src/app/api/chat/route.test.ts` — all tests still pass
</verification>

<success_criteria>
- supabase/migrations/20260303000001_add_checkin_checkout_house_rules.sql exists with all three columns
- src/app/api/chat/property-context.ts exports buildPropertyContext and PropertyRecord
- All property-context.test.ts tests pass including null-field, label mapping, truncation, and never-invent instruction tests
- Existing route.test.ts tests unaffected (no imports changed in route.ts yet)
</success_criteria>

<output>
After completion, create `.planning/phases/04-property-faq-integration/04-01-SUMMARY.md`
</output>
