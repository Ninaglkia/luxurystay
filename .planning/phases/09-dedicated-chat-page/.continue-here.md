---
phase: post-milestone-debug
task: 1
total_tasks: 1
status: blocked
last_updated: 2026-03-03T22:49:01.882Z
---

<current_state>
ALL 9 PHASES ARE COMPLETE. The milestone (v1.0 AI Concierge) is fully built and deployed.
The site is live at https://luxurystay-alpha.vercel.app but /api/chat returns 500.
The homepage loads correctly (middleware fix applied — lazy import of ratelimit).
The chat bubble appears and sends messages, but gets no AI response.
</current_state>

<completed_work>

- Phase 1: AI API Foundation — streaming /api/chat endpoint ✓
- Phase 2: Security Hardening — rate limiting + injection detection ✓
- Phase 3: Access Control — header stripping security fix ✓
- Phase 4: Property FAQ Integration — DB migration + property context ✓
- Phase 5: Booking Support — booking context + availability ✓
- Phase 6: Concierge Recommendations — location-aware prompt engineering ✓
- Phase 7: Chat Persona & UX — luxury persona + useChat + streaming UI ✓
- Phase 8: Chat Bubble Widget — lazy-loaded FAB on every page ✓
- Phase 9: Dedicated Chat Page — full-screen /chat with auto-scroll ✓
- Upstash Redis credentials added to .env.local AND Vercel env vars ✓
- Middleware fix: lazy import of ratelimit to prevent MIDDLEWARE_INVOCATION_FAILED ✓
- npm run build passes locally ✓
- Vercel deploy succeeds ✓

</completed_work>

<remaining_work>

- Debug why POST /api/chat returns 500 on Vercel production
- The Anthropic API key WORKS (verified via direct curl call — got "Ciao! Come stai?")
- The error is in the route.ts runtime on Vercel, not the API key

</remaining_work>

<decisions_made>

- Lazy import of ratelimit in proxy.ts (dynamic import only on /api/chat path) — fixed MIDDLEWARE_INVOCATION_FAILED for all routes
- Upstash Redis region: EU-West-1 (Belgium, GCP) — closest to Italy

</decisions_made>

<blockers>

- /api/chat returns 500 Internal Server Error on Vercel production
- Anthropic API key has credits and works (confirmed via direct curl)
- Could NOT retrieve Vercel runtime logs (vercel logs command hangs)
- Likely causes to investigate:
  1. `admin-supabase.ts` import or `getAdminSupabase()` crashing at module scope
  2. `@ai-sdk/anthropic` or `ai` package edge runtime incompatibility
  3. `convertToModelMessages` async behavior issue on Vercel
  4. Missing `SUPABASE_SERVICE_ROLE_KEY` or `NEXT_PUBLIC_SUPABASE_URL` on Vercel for non-production environments
- Debug approach: Add try/catch around the entire POST handler and log the error, or test with a minimal route that only calls Anthropic without Supabase

</blockers>

<context>
The entire AI concierge chatbot codebase is complete (9 phases, 110+ tests locally).
The only issue is a production runtime error in /api/chat on Vercel.
All env vars are set on Vercel (checked with `vercel env ls`).
The build succeeds — this is a RUNTIME error, not a build error.
Start debugging by simplifying route.ts or checking Vercel function logs via the dashboard.
</context>

<next_action>
Start with: Check Vercel dashboard Function logs (vercel.com → project → Deployments → latest → Functions tab → /api/chat) to see the actual error message. Alternatively, temporarily wrap the entire POST handler in a try/catch that returns the error as JSON to identify the exact failure point.
</next_action>
